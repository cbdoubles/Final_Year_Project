// import { fetchFolders } from "@/utils/apiCalls/folder/fetchFolders";

describe("template spec", () => {
  beforeEach(() => {
    //   cy.visit("/projectpage");
    //   cy.intercept('GET', 'http://localhost:8000/api/folders/by-project/**', (req) => {
    //     // Provide a mocked response
    //     req.reply({
    //       statusCode: 200,
    //       body: [
    //         { id: 1, name: 'Folder 1', type: 'TYPE_1' },
    //         { id: 2, name: 'Folder 2', type: 'TYPE_2' },
    //       ],
    //     });
    //   }).as('fetchFolders');
    // });
    // it('should fetch and transform folders correctly', () => {
    //   // Call the fetchFolders function
    //   cy.window().then(async (win) => {
    //     const fetchFolders = win.fetchFolders;
    //     const result = await fetchFolders(1, 'TYPE_1');
    //     expect(result).to.deep.equal([
    //       { folderId: 1, folderName: 'Folder 1', folderType: 'TYPE_1' },
    //       { folderId: 2, folderName: 'Folder 2', folderType: 'TYPE_2' },
    //     ]);
    //   });
  });
});
